export interface <%= domainNames.pascalCase %>Request {
  name: string;
  description?: string;
  isActive?: boolean;
}

export interface <%= domainNames.pascalCase %>Response {
  id: string;
  name: string;
  description?: string;
  isActive: boolean;
  createdAt: Date;
  updatedAt: Date;
}

export interface Create<%= domainNames.pascalCase %>Request {
  name: string;
  description?: string;
  isActive?: boolean;
}

export interface Update<%= domainNames.pascalCase %>Request {
  name?: string;
  description?: string;
  isActive?: boolean;
}
